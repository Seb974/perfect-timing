<form method="post" action="{{ path('eco_level_delete', {'id': eco_level.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eco_level.id) }}">
    <button class="btn">Delete</button>
</form>
