<form method="post" action="{{ path('host_place_delete', {'id': host_place.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ host_place.id) }}">
    <button class="btn">Delete</button>
</form>
