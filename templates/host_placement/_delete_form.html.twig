<form method="post" action="{{ path('host_placement_delete', {'id': host_placement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ host_placement.id) }}">
    <button class="btn">Delete</button>
</form>
